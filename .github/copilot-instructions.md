# PMS Pharmacy v4 — AI Agent Instructions

> This project uses MULTIPLE AI agents. Read `docs/AGENT-HANDOFF.md` at the start of every session.
> Update it at the end of every session with what you built, what broke, and what's next.
> NEVER create other plan files.

See `.cursorrules` in project root for full coding rules.
